<?xml version="1.0" encoding="utf-8"?>
<!-- Resizable from 2x2 (the month) up. CalendarWidget picks the small,
     medium or large layout for the size the launcher gives it. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="110dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="10800000"
    android:initialLayout="@layout/calendar_widget_medium"
    android:previewImage="@drawable/calendar_widget_preview"
    android:description="@string/calendar_widget_description"
    android:widgetCategory="home_screen" />
