<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:scheduler="clr-namespace:Syncfusion.Maui.Scheduler;assembly=Syncfusion.Maui.Scheduler"               
             xmlns:controls="clr-namespace:Ifpa.Views.Controls"
             x:Class="Ifpa.Views.CalendarPage"
             xmlns:fluent="clr-namespace:MauiIcons.Fluent;assembly=MauiIcons.Fluent"
             Title="Calendar">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="scheduler:SchedulerTextStyle" x:Key="CalendarHeaderStyle">
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource PrimaryTextColor}, Dark={StaticResource PrimaryTextColorDark}}" />
                <Setter Property="FontSize" Value="200"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="My Location" Clicked="MyLocation_Clicked" IconImageSource="{fluent:Icon Icon=Location48, IconColor={StaticResource IconAccentColor}}"  />
        <ToolbarItem Text="Toggle View" Clicked="ToggleView_Clicked" IconImageSource="{fluent:Icon Icon=CalendarLtr28, IconColor={StaticResource IconAccentColor}}" />
    </ContentPage.ToolbarItems>
    <Grid>
        <Grid x:Name="MapLayout" RowDefinitions="*,*" >

            <Map x:Name="calendarMap" MapType="Street" Grid.Row="0" />
      
                <CollectionView x:Name="TournamentListView" 
                                 Grid.Row="1"
                    ItemsSource="{Binding CalendarDetails}"         
                    SelectionMode="Single"
                    SelectionChanged="TournamentListView_SelectionChanged">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="10" ColumnSpacing="10" ColumnDefinitions="*,90">
                                <Label Grid.Column="0" Text="{Binding TournamentName}" />
                                <Label Grid.Column="1" MinimumWidthRequest="90" FontSize="Small" VerticalTextAlignment="Center" HorizontalTextAlignment="End" TextColor="{DynamicResource SecondaryTextColor}" 
                                       Text="{Binding EndDate, StringFormat='{0:d}'}" LineBreakMode="NoWrap" />
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
     
        </Grid>
        <scheduler:SfScheduler x:Name="calendar" IsVisible="False" VerticalOptions="FillAndExpand" 
                                MinimumDateTime="{Binding DateTime.Today}"
                                View="Month"                                                               
                                AppointmentsSource="{Binding TournamentCalenderItems, Mode=OneWay}" 
                               Tapped="calendar_Tapped"
                                BackgroundColor="{AppThemeBinding Light={StaticResource BackgroundColor}, Dark={StaticResource BackgroundColorDark}}">
            <scheduler:SfScheduler.MonthView>
                <scheduler:SchedulerMonthView>
                    <scheduler:SchedulerMonthView.ViewHeaderTemplate>
                        <DataTemplate>
                            <Grid>
                                <Label x:Name="label" HorizontalOptions="Center" VerticalOptions="Center" Text="{Binding StringFormat='{0:ddd}'}" 
                                       TextColor="{AppThemeBinding Light={StaticResource PrimaryTextColor}, Dark={StaticResource PrimaryTextColorDark}}" 
                                       FontSize="Subtitle" FontFamily="Bold" />
                            </Grid>
                        </DataTemplate>
                    </scheduler:SchedulerMonthView.ViewHeaderTemplate>
                    <scheduler:SchedulerMonthView.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <Label HorizontalTextAlignment="Center" TextColor="{AppThemeBinding Light={StaticResource PrimaryTextColor}, Dark={StaticResource PrimaryTextColorDark}}" Text="{Binding DateTime.Day}"/>
                            </Grid>
                        </DataTemplate>
                    </scheduler:SchedulerMonthView.CellTemplate>
                </scheduler:SchedulerMonthView>
            </scheduler:SfScheduler.MonthView>
        </scheduler:SfScheduler>
    </Grid>
</ContentPage>
